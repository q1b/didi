---

import CourseCard from './CourseCard.astro';

const { allProjects } = Astro.props;

---
{allProjects.map(p => 
  <CourseCard
    title={p.title} 
    subTitle={p.subTitle}
    link={p.url}
    days={p.days}
    hours={p.hours}
    bg={p.bg}
  />
)}
